dashboard.html:

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSMS - Core Intelligence Dashboard</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="assets/css/style.css">
</head>
<body class="bg-[#0b0f19] text-slate-100 min-h-screen font-sans cyber-grid antialiased">

    <nav class="glass-panel border-b border-slate-800/80 sticky top-0 z-50 px-6 py-4 shadow-2xl flex justify-between items-center">
        <div class="flex items-center gap-3">
            <div class="h-3 w-3 rounded-full bg-emerald-500 animate-pulse shadow-[0_0_10px_#10b981]"></div>
            <div>
                <h1 class="text-xl font-black tracking-widest text-transparent bg-clip-text bg-gradient-to-r from-indigo-400 to-cyan-400 uppercase">⚡ CSMS Intelligence</h1>
                <p class="text-[10px] font-mono text-slate-500 uppercase tracking-widest">Neon Relational Data Engine</p>
            </div>
        </div>
        <a href="index.html" class="text-xs font-mono border border-slate-700 hover:border-indigo-500 bg-slate-900/50 px-4 py-2 rounded-xl transition">&larr; PORTAL ESCAPE</a>
    </nav>

    <main class="container mx-auto my-10 px-4 max-w-7xl grid grid-cols-1 lg:grid-cols-2 gap-8">
        <div class="glass-panel p-6 rounded-3xl relative overflow-hidden neon-glow-indigo">
            <h2 class="text-sm font-mono font-bold text-slate-300 uppercase mb-4 border-b border-slate-800 pb-2">💰 Financial Ingestion Matrix (STUDENT_INVOICES)</h2>
            <div class="relative h-80 w-full"><canvas id="feeStatusChart"></canvas></div>
        </div>

        <div class="glass-panel p-6 rounded-3xl relative overflow-hidden neon-glow-indigo">
            <h2 class="text-sm font-mono font-bold text-slate-300 uppercase mb-4 border-b border-slate-800 pb-2">📊 Structural Density Map (ENROLLMENTS)</h2>
            <div class="relative h-80 w-full"><canvas id="studentDensityChart"></canvas></div>
        </div>
    </main>

    <script src="assets/js/dashboard.js"></script>
</body>
</html>

fee.html:

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSMS - Ledger Influx</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="assets/css/style.css">
</head>
<body class="bg-[#0b0f19] text-slate-100 min-h-screen font-sans cyber-grid flex flex-col justify-between">

    <nav class="glass-panel border-b border-slate-800/80 px-6 py-4 flex justify-between items-center">
        <h1 class="text-lg font-black tracking-widest text-cyan-400 uppercase">💰 Billing Account System</h1>
        <a href="index.html" class="text-xs font-mono border border-slate-700 hover:border-indigo-500 bg-slate-900/50 px-4 py-2 rounded-xl transition">&larr; ESCAPE</a>
    </nav>

    <main class="container mx-auto my-auto px-4 py-12 max-w-2xl">
        <div class="glass-panel p-8 rounded-3xl neon-glow-cyan">
            <div class="mb-6 border-b border-slate-800 pb-3">
                <h2 class="text-base font-bold text-slate-200 uppercase">📝 Issue Dynamic Fee Invoice Document</h2>
                <p class="text-[10px] font-mono text-slate-500 mt-0.5">TARGET DATA KEY: PUBLIC.STUDENT_INVOICES</p>
            </div>
            
            <form id="feeGenerationForm" class="space-y-5">
                <input type="number" id="refStudentId" required placeholder="Student DB Relational ID (FK)" class="w-full bg-slate-900/40 border border-slate-800 focus:border-cyan-500 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none">
                <input type="number" step="0.01" id="invoiceAmount" required placeholder="Invoice Gross Amount (PKR)" class="w-full bg-slate-900/40 border border-slate-800 focus:border-cyan-500 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none">
                <input type="date" id="dueDate" required class="w-full bg-slate-900/40 border border-slate-800 focus:border-cyan-500 rounded-xl px-4 py-3 text-sm text-slate-400 focus:outline-none">
                
                <select id="paymentStatus" required class="w-full bg-slate-900/60 border border-slate-800 focus:border-cyan-500 rounded-xl px-4 py-3 text-sm text-slate-300 focus:outline-none cursor-pointer">
                    <option value="Unpaid">Unpaid (Pending Queue)</option>
                    <option value="Paid">Paid (Transaction Cleared)</option>
                    <option value="Partially Paid">Partially Paid</option>
                </select>

                <button type="submit" class="w-full bg-gradient-to-r from-cyan-600 to-cyan-400 text-white font-mono font-bold text-xs tracking-widest py-4 rounded-xl transition duration-500 uppercase cursor-pointer">🚀 Process Transaction Influx</button>
            </form>
            <div id="feeFeedback" class="hidden mt-4 p-3 rounded-xl text-center text-xs font-mono"></div>
        </div>
    </main>

    <script src="assets/js/fee.js"></script>
</body>
</html>

index.html:

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSMS - Core Matrix Gate</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="assets/css/style.css">
</head>
<body class="bg-[#0b0f19] text-slate-100 min-h-screen font-sans cyber-grid flex flex-col justify-between">

    <header class="glass-panel border-b border-slate-800/80 px-6 py-5 text-center shadow-2xl">
        <h1 class="text-2xl font-black tracking-widest text-transparent bg-clip-text bg-gradient-to-r from-indigo-400 via-cyan-400 to-emerald-400 uppercase">
            🏫 Comprehensive School Management System
        </h1>
        <p class="text-[10px] font-mono text-slate-500 uppercase tracking-widest mt-1">Enterprise Relational Interface Network</p>
    </header>

    <main class="container mx-auto px-4 max-w-5xl my-auto grid grid-cols-1 md:grid-cols-4 gap-6 py-12">
        <a href="dashboard.html" class="glass-panel p-6 rounded-2xl hover:border-indigo-500 neon-glow-indigo transition duration-500 group">
            <div class="text-2xl mb-3 group-hover:scale-110 transition-transform">📊</div>
            <h2 class="font-mono font-bold text-xs uppercase tracking-wider text-indigo-400">Core Analytics</h2>
            <p class="text-[11px] text-slate-400 mt-2">Live chart rendering vectors from database pools.</p>
        </a>

        <a href="student.html" class="glass-panel p-6 rounded-2xl hover:border-indigo-500 neon-glow-indigo transition duration-500 group">
            <div class="text-2xl mb-3 group-hover:scale-110 transition-transform">🎒</div>
            <h2 class="font-mono font-bold text-xs uppercase tracking-wider text-indigo-400">Student Ingestion</h2>
            <p class="text-[11px] text-slate-400 mt-2">Commit admission and class mapping write-streams.</p>
        </a>

        <a href="teacher.html" class="glass-panel p-6 rounded-2xl hover:border-emerald-500 neon-glow-emerald transition duration-500 group">
            <div class="text-2xl mb-3 group-hover:scale-110 transition-transform">👩‍🏫</div>
            <h2 class="font-mono font-bold text-xs uppercase tracking-wider text-emerald-400">Faculty Influx</h2>
            <p class="text-[11px] text-slate-400 mt-2">Register staff identifiers to 3NF architecture tables.</p>
        </a>

        <a href="fee.html" class="glass-panel p-6 rounded-2xl hover:border-cyan-500 neon-glow-cyan transition duration-500 group">
            <div class="text-2xl mb-3 group-hover:scale-110 transition-transform">💰</div>
            <h2 class="font-mono font-bold text-xs uppercase tracking-wider text-cyan-400">Ledger Posting</h2>
            <p class="text-[11px] text-slate-400 mt-2">Generate invoices and monitor payment lifecycles.</p>
        </a>
    </main>

    <footer class="text-center py-4 text-[9px] font-mono text-slate-600 tracking-widest uppercase">
        Node Session Status: Secure // Developer Mode Active
    </footer>
</body>
</html>

student.html:

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSMS - Student Ingestion Node</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="assets/css/style.css">
</head>
<body class="bg-[#0b0f19] text-slate-100 min-h-screen font-sans cyber-grid flex flex-col justify-between">

    <nav class="glass-panel border-b border-slate-800/80 px-6 py-4 flex justify-between items-center">
        <h1 class="text-lg font-black tracking-widest text-indigo-400 uppercase">🎒 Identity Ingestion Layer</h1>
        <a href="index.html" class="text-xs font-mono border border-slate-700 hover:border-indigo-500 bg-slate-900/50 px-4 py-2 rounded-xl transition">&larr; ESCAPE</a>
    </nav>

    <main class="container mx-auto my-auto px-4 py-12 max-w-2xl">
        <div class="glass-panel p-8 rounded-3xl neon-glow-indigo">
            <div class="mb-6 border-b border-slate-800 pb-3">
                <h2 class="text-base font-bold text-slate-200 uppercase">📝 Register New Academic Entity</h2>
                <p class="text-[10px] font-mono text-slate-500 mt-0.5">TARGET DATA KEY: PUBLIC.STUDENTS</p>
            </div>
            
            <form id="studentRegistrationForm" class="space-y-5">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <input type="text" id="firstName" required placeholder="First Name" class="w-full bg-slate-900/40 border border-slate-800 focus:border-indigo-500 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none">
                    <input type="text" id="lastName" required placeholder="Last Name" class="w-full bg-slate-900/40 border border-slate-800 focus:border-indigo-500 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none">
                </div>
                <input type="email" id="studentEmail" required placeholder="Institutional Email" class="w-full bg-slate-900/40 border border-slate-800 focus:border-indigo-500 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none">
                <input type="tel" id="studentPhone" required placeholder="Phone Contact" class="w-full bg-slate-900/40 border border-slate-800 focus:border-indigo-500 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none">
                
                <select id="studentClassSelection" required class="w-full bg-slate-900/60 border border-slate-800 focus:border-indigo-500 rounded-xl px-4 py-3 text-sm text-slate-300 focus:outline-none cursor-pointer">
                    <option value="" disabled selected>Querying segments from cluster...</option>
                </select>

                <button type="submit" class="w-full bg-gradient-to-r from-indigo-600 to-indigo-400 text-white font-mono font-bold text-xs tracking-widest py-4 rounded-xl transition duration-500 uppercase cursor-pointer">🚀 Execute DB Commit</button>
            </form>
            <div id="submissionFeedback" class="hidden mt-4 p-3 rounded-xl text-center text-xs font-mono"></div>
        </div>
    </main>

    <script src="assets/js/student.js"></script>
</body>
</html>

teacher.html:

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSMS - Faculty Node</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="assets/css/style.css">
</head>
<body class="bg-[#0b0f19] text-slate-100 min-h-screen font-sans cyber-grid flex flex-col justify-between">

    <nav class="glass-panel border-b border-slate-800/80 px-6 py-4 flex justify-between items-center">
        <h1 class="text-lg font-black tracking-widest text-emerald-400 uppercase">👩‍🏫 Faculty Profile Pipeline</h1>
        <a href="index.html" class="text-xs font-mono border border-slate-700 hover:border-indigo-500 bg-slate-900/50 px-4 py-2 rounded-xl transition">&larr; ESCAPE</a>
    </nav>

    <main class="container mx-auto my-auto px-4 py-12 max-w-2xl">
        <div class="glass-panel p-8 rounded-3xl neon-glow-emerald">
            <div class="mb-6 border-b border-slate-800 pb-3">
                <h2 class="text-base font-bold text-slate-200 uppercase">📝 Register New Faculty Record</h2>
                <p class="text-[10px] font-mono text-slate-500 mt-0.5">TARGET DATA KEY: PUBLIC.TEACHERS</p>
            </div>
            
            <form id="teacherRegistrationForm" class="space-y-5">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <input type="text" id="tFirstName" required placeholder="First Name" class="w-full bg-slate-900/40 border border-slate-800 focus:border-emerald-500 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none">
                    <input type="text" id="tLastName" required placeholder="Last Name" class="w-full bg-slate-900/40 border border-slate-800 focus:border-emerald-500 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none">
                </div>
                <input type="email" id="teacherEmail" required placeholder="Staff Email Address" class="w-full bg-slate-900/40 border border-slate-800 focus:border-emerald-500 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none">
                <input type="tel" id="teacherPhone" required placeholder="Phone Identity" class="w-full bg-slate-900/40 border border-slate-800 focus:border-emerald-500 rounded-xl px-4 py-3 text-sm text-slate-200 focus:outline-none">
                <input type="date" id="hireDate" required class="w-full bg-slate-900/40 border border-slate-800 focus:border-emerald-500 rounded-xl px-4 py-3 text-sm text-slate-400 focus:outline-none">

                <button type="submit" class="w-full bg-gradient-to-r from-emerald-600 to-emerald-400 text-white font-mono font-bold text-xs tracking-widest py-4 rounded-xl transition duration-500 uppercase cursor-pointer">🚀 Commit Faculty Entry</button>
            </form>
            <div id="teacherFeedback" class="hidden mt-4 p-3 rounded-xl text-center text-xs font-mono"></div>
        </div>
    </main>

    <script src="assets/js/teacher.js"></script>
</body>
</html>
 this is all i have. i want to make full scale modern comperehensive school management database and want to connect with github codespace and neon for database and want to live this in web server with the use of neon